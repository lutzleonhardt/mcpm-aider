import type { MCPServerWithStatus } from '@mcpm/sdk';

export async function generateToolPrompt(
  servers: MCPServerWithStatus
): Promise<string> {
  // Minimal scaffolding - user to implement
  return '';
}
